{{> privateNavbar}}
<main>
<div class='jumbotron text-center'>
  <h1 class='display-4'>Welcome {{user.name}}</h1>
  <p class='lead'>Here are a list of your projects</p>
  <a class="btn btn-primary btn-lg" href="/create-project" role="button"><span>Add Project</span> <i class="fa-solid fa-plus mx-1"></i></a>
</div>
<div class="d-flex flex-row justify-content-around flex-wrap">
  {{#each projects as |  project |}}
  {{>projectCard}}
  {{/each}}
</div>
</main>